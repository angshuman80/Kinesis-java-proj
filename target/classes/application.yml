aws:
  region: us-east-1
  kinesis:
    stream-name: your-kinesis-stream-name
    application-name: kinesis-dynamodb-consumer
  dynamodb:
    table-name: your-dynamodb-table-name

spring:
  application:
    name: kinesis-dynamodb-consumer

logging:
  level:
    root: INFO
    com.example.kinesis: DEBUG
    software.amazon.kinesis: INFO
